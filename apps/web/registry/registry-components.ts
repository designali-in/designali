import type { Registry } from "@/registry/schema";

export const components: Registry = [
  // Safari
  {
    name: "button-01",
    type: "registry:components",
    files: ["components/buttons/button-01.tsx"],
  },
];
